from "stdio.cpl" import puts putc;

function memset ptr char buffer; int val; long size; {
    long index = 0;
    while index < size; {
        buffer[index] = val;
        index = index + 1;
    }

    return 1;
} 

start
:
    arr hello_str 0 char = { 'H', 'e', 'l', 'l', 'o', ' ', 'w', 'o', 'r', 'l', 'd', 10, 0 };
    puts(hello_str);
:
    arr hello_str 0 char = { 'H', 'e', 'l', 'l', 'o', ' ', 'w', 'o', 'r', 'l', 'd', 10, 0 };
    int a = 0;
    int b = 10;
    int c = a * c + (a * b);
    putc('H');
    putc(0);
    memset(0, 0, 10);
exit 1 + 1;