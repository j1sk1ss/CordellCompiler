{
    function fib(i32 a) => i32 {
        i32 b = a + 10;
        if b >= 10; {
            return b;
        }

        return fib(b);
    }

    start(i64 argc, ptr u64 argv) {   
        exit fib(1);
    }
}