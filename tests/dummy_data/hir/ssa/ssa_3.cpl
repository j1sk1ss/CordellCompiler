{
    start(i64 argc, ptr u64 argv) {
        i32 a = 10;
        while a > 0; {
            i32 b = 10;
            while b > 0; {
                i32 c = a + b * (10 - 20);
                b -= 1;
            }

            a -= 1;
        }
        
        exit 0;
    }
}