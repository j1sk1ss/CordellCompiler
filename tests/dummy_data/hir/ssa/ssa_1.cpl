{
    function sub(i32 a, i32 b) => i32 {
        return sub(a, b);
    }

    function sum(i32 a, i32 b) => i32 {
        return a + b;
    }

    start(i64 argc, ptr u64 argv) {
        u8 a = sum(10, 10);
        exit 0;    
    }
}