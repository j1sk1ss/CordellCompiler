{
    function foo(u32 a, f64 b) => f64 {
        return b;
    }

    start(i64 argc, ptr u64 argv) {
        i32 a = 10;
        if a == 20; {
            a = 10;
        }

        i32 c = 20;
        i32 d = 20;
        if d == 20; {
            d = 10;
        }

        i32 k = c;
        exit k;
    }
}