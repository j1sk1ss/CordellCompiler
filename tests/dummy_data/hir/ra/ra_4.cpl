{
    function foo() => i32 { return 0; }
    start(i64 argc, ptr u64 argv) {
        i8 a = 10;
        ptr i8 b = ref a;
        ptr i8 c = ref b;
        if a && foo(); {
            a = 12;
        }

        exit b;
    }
}