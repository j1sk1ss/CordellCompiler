{
    function foo(i32 n) => i32 {
        i32 z = 10;
        i32 x = 0;
        i32 y = 1;

        while x < n; {
            z = x * 2 + y;
            x += 1;
            y = x + z;
        }

        return y;
    }
}