{
    start(i64 argc, ptr u64 argv) {
        i32 a;
        i32 b;
        i32 c;
        i32 g;
        i32 e;
        i32 d;
        a = b * c + g;
        d = b * c * e;
        exit a + d;
    }
}