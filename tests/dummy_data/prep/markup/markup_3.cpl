{
    function foo() => i32 {
        return 0;
    }

    function _foo() => () {
        return;
    }

    function bar() => ptr u8 {
        return "0";
    }

    function test() => ptr u64 {
        glob arr test[100, u64] =;
        return test;
    }

    start(i64 argc, ptr u64 argv) => u8 {
        exit 0;
    }
}