{
    start() {
        int a = 10;
        int b = 20;
        int c = a + b;
        int d = c * a;

        short k = a;

        int f = 0;
        f = a;
        f = b;

        int g = f + a + b;

        if a; {
            f = b;
        }
        else {
            f = a;
        }

        while (a - b) > 0; {
            a = a + a;
            b = b + b;
        }

        switch a + b; {
            case c; {
                a = b;
            }
            case d; {
                b = a;
            }
            default {
                f = b + a;
            }
        }

        exit c;
    }
}