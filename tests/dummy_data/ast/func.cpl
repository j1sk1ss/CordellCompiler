{
    function pow(i32 num, i8 power) => u64 {
        return 1;
    }

    glob function foo(i32 a) => i32 {
        return a;
    }

    function void() {
        return;
    }

    start() {
        i32 b = 10;
        foo(1);
        void();
        exit pow(b, 2);
    };
}