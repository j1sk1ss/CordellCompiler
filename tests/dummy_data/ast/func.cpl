{
    function pow(i32 num = 1, i8 power = 2) => u64 {
        return 1;
    }

    glob function foo(i32 a = 1) => i32 {
        return a;
    }

    function void() {
        return;
    }

    start() {
        pow();
        pow(10);
        pow(10, 20);

        foo();
        foo(10);

        void();
        exit 0;
    };
}