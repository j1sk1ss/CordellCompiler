{
    extern i32 size;
    start(i64 argc, ptr u64 argv) {
        
        arr a[size, i32];

        {
            arr b[size, i32];
            
            {
                arr c[size, i32];
            }
        }

        {
            arr d[size, i32];
        }

        exit 0;
    }
}