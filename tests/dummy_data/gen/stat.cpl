{
    glob arr array[100, char] =;
    function strlen(ptr char string) {
        int length = 0;
        while string[0]; {
            length = length + 1;
            string = string + 1;
        }

        return length;
    }
}