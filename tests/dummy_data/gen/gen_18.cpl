{
    start(i64 argc, ptr u64 argv) {
        arr a1[2, i32];
        arr a2[2, i32];
        arr sq[2, ptr i32] = { ref a1, ref a2 };
        
        ptr i32 el = sq[0];
        el[0] = 1;
        exit 0;
    }
}