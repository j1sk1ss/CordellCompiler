{
    function pow(i32 num, i8 power) {
        return 1;
    }

    glob function foo(i32 a) {
        return a;
    }

    function void() {
        return;
    }

    start() {
        i32 b = 10;
        foo(1);
        void();
        exit pow(b, 2);
    };
}