{
    function pow(int num, char power) {
        return 1;
    }

    glob function foo(int a) {
        return a;
    }

    function void() {
        return;
    }

    start() {
        int b = 10;
        foo(1);
        void();
        exit pow(b, 2);
    };
}