{
    extern ptr u8 frame_buffer;
    extern exfunc printf;

    function foo(i32 a = 10) {
        return 0;
    }

    start() {
        frame_buffer[0] = 'R';
        printf("Hello there! %s", "Hello!!!!");
        exit foo();
    }
}