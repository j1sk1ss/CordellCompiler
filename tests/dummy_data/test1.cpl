{
    struct test_struct {
        i32    a;
        u8     b;
        arr    c[10, i32];
        ptr u8 buffer;
    }

    start(i64 argc, ptr u64 argv) {
        test_struct a;
        a.a = 10;

        arr b[2, test_struct];
        b[0].a = 10;

        exit 0;
    }
}