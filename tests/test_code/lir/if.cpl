{
    start() {
        if 1; {
        }
        else {
        }
    }
}

: OUTPUT
BB{X}: start
cmp $1, 0;
je lb{X};
jne lb{X};

BB{X}: lb{X}:
jmp lb{X};

BB{X}: lb{X}:
jmp lb{X};

BB{X}: lb{X}:
send
: