{
    from "string.cpl" import strlen;

    function putc char sym; {
        return 1;
    }

    function puts ptr char string; {
        long strSize = strlen(string);
        return syscall(1, 1, string, strSize);
    }

    start {
        arr str_arr 0 char = { 'H', 'e', 'l', 'l', 'o', ' ', 'w', 'o', 'r', 'l', 'd', 0 };
        int a = 0;
        int b = 10;
        int c = (a + c) * a;
        
        {
            short asd = 0;
            short sdf = 1;
            short dfg = 2;
        }

        char asd = 0;
        char sdf = 1;
        char dfg = 2;

        if sdf == 1; {
            char utu = 123;
        }

        long rer = 345;
        long utu = 321;

        putc('H'); putc(0); : One line comment and one line commands! :
        puts("Hello there from puts!");

        str_arr = a;
        a = str_arr[0];
    } exit c;
}