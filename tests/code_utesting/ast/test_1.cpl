{
    extern function printf(ptr i8 fmt, ...);
    start(i64 argc, ptr u64 argv) {
        
        i32 num = 5;
        i32 factorio = 1;


        while num > 0; {
            factorio * num;
            num -= 1;
        }


        printf("Hello! %s\n", "String!");
        printf("%d", factorio);
        exit 0;
    }
}