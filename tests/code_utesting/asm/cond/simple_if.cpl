start() {
    if 1; {
        exit 1;
    }
    else {
        exit 2;
    }
}

:/ OUTPUT
; Compiled .asm file. File generated by CordellCompiler!
section .text
global _main
_main:
    mov     al, 1
    cmp     al, 0
    je      lb8
    jne     lb7
; BB2:
lb7:
    mov     cl, 1
    mov     dil, cl
    mov     rax, 60
    syscall
; BB3:
lb8:
    mov     cl, 2
    mov     dil, cl
    mov     rax, 60
    syscall
; BB4:
lb9:
    mov     rax, 60
    syscall
/: