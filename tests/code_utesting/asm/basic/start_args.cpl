start(i32 argc, ptr ptr i8 argv) {
    exit 0;
}

:/ OUTPUT
; Compiled .asm file. File generated by CordellCompiler!
section .text
global _main
_main:
    push    rbp
    mov     rbp, rsp
    mov     ecx, qword [rbp + 8]
    lea     rcx, [rbp + 16]
    mov     cl, 0
    mov     dil, cl
    mov     rax, 60
    syscall
; BB2:
    mov     rax, 60
    syscall
/: