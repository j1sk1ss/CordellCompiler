function foo(i32 a) -> i32 {
    return a + 10;
}

start() {
    exit foo(10) as u8;
}

:/ OUTPUT
; Compiled .asm file. File generated by CordellCompiler!
section .text
global _main
_main:
    push    rdi
    mov     dil, 10
    call    _cpl_foo0
    pop     rdi
    mov     ecx, eax
    mov     cl, cl
    mov     dil, cl
    mov     rax, 60
    syscall
; BB5:
    mov     rax, 60
    syscall
_cpl_foo0:
    mov     edx, 10
    mov     eax, ecx
    add     eax, edx
    mov     ecx, eax
    mov     eax, ecx
    ret
; BB3:
    ret
/: