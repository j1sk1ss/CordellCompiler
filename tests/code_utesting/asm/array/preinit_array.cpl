glob arr a[5, i32] = { 0, 1, 2, 3, 4 };
start() {
    exit a[0] as u8;
}

:/ OUTPUT
; Compiled .asm file. File generated by CordellCompiler!
section .data
a                        dd      0,1,2,3,4
section .text
global _main
_main:
    lea     rbx, qword [rel a]
    mov     eax, 0
    imul    eax, 4
    mov     cl, eax
    movsx   rdx, cl
    mov     rax, rbx
    add     rax, rdx
    mov     rcx, rax
    mov     ecx, [rcx]
    mov     cl, cl
    mov     dil, cl
    mov     rax, 60
    syscall
; BB2:
    mov     rax, 60
    syscall
/: