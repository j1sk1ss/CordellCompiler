glob arr a[10, i32];
start() {
    a[0] = 1 as i32;
}

:/ OUTPUT
; Compiled .asm file. File generated by CordellCompiler!
section .data
a                        resd    10
section .text
global _main
_main:
    mov     esi, 1
    lea     rbx, qword [rel a]
    mov     eax, 0
    imul    eax, 4
    mov     cl, eax
    movsx   rdx, cl
    mov     rax, rbx
    add     rax, rdx
    mov     rcx, rax
    mov     dword [rcx], esi
    mov     rax, 60
    syscall
/: