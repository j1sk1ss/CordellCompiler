{
    #include "print_h.cpl"
    #include "string_h.cpl"

    function foo() -> i0;
    
    function bar() -> i0 {
        foo();
    }

    function foo() -> i0 {
        print("Hello world!\n");
    }

    start(i64 argc, ptr u64 argv) {
        bar();
        exit 0;
    }
}