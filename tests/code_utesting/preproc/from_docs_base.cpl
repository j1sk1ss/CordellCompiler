#include "print_h.cpl"

start(i64 argc, ptr u64 argv) {
    str msg = "Hello world!";
    print(ref msg);
    exit 0;
}

:/ OUTPUT
#line 0 "{X}"
#line 0 "{X}"
function strlen(ptr i8 s) -> i64;
#line 3 "{X}"
function print(ptr i8 msg) -> i0;
#line 1 "{X}"
start(i64 argc, ptr u64 argv) {
    str msg = "Hello world!";
    print(ref msg);
    exit 0;
}
/:
