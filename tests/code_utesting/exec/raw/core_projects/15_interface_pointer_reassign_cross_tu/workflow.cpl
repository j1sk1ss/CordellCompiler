#include "workflow.inc"
function switch15(ptr value15 first, ptr value15 second) -> i32 {
    ptr value15 current = first;
    i32 ignored = current.value();
    current = second;
    return current.value();
}
