#include "aggregate.inc"
function total20(ptr value20 first, ptr value20 second) -> i32 {
    arr items[2, ptr value20];
    items[0] = first;
    items[1] = second;
    return items[0].value() + items[1].value();
}
