#include "extern_math.inc"

function dispatch07() -> i32 {
    ptr fn(i32,i32)i32 op = ext_add07;
    i32 total = op(20 as i32, 10 as i32);

    op = ext_sub07;
    total += op(20 as i32, 8 as i32);

    return total;
}
