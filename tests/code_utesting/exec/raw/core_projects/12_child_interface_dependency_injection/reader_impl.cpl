#include "reader_impl.inc"

function reader_impl12::value(ptr reader_impl12 self) -> i32 {
    return self.base + self.offset;
}

function reader_impl12::set_offset(ptr reader_impl12 self, i32 offset) -> i0 {
    self.offset = offset;
}
