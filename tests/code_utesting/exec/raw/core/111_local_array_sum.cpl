: RUN_ASM :

start() {
    arr values[5, i32] = { 2, 4, 6, 8, 10 };
    i32 i = 0;
    i32 sum = 0;

    while i < 5; {
        sum += values[i];
        i += 1;
    }

    exit sum as u8;
}

:/ OUTPUT
@exit_code=30
/:
