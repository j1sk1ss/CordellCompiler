start() {
    arr a[10, i32];
    i32 b = a[5];
}

:/ OUTPUT
BB1: start
rcx = &([rbp - 40]);
rax = $5;
rax = rax * 4;
rsi movsx rax;
rbx = rsi;
rax = rcx;
rax = rax + rbx;
rdi = rax;
rdx = *(rdi);
r8 = rdx;
kill(7);
send
/: