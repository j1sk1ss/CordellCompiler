start() {
    arr a[10, i32];
    i32 b = a[5];
}

:/ OUTPUT
BB1: start
rbx = &([rbp - 40]);
rax = $5;
rax = rax * 4;
rcx = rax;
rdx movsx rcx;
rax = rbx;
rax = rax + rdx;
rcx = rax;
rcx = *(rcx);
rcx = rcx;
send
/: