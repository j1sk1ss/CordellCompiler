start() {
    arr a[10, i32];
    a[5] = 0 as i32;
}

:/ OUTPUT
BB1: start
rsi = $0;
rdi = &([rbp - 40]);
rax = $5;
rax = rax * 4;
rdx movsx rax;
rcx = rdx;
rax = rdi;
rax = rax + rcx;
rbx = rax;
*(rbx) = rsi;
kill(6);
send
/: