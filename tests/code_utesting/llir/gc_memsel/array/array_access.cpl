start() {
    arr a[10, i32];
    a[5] = 0 as i32;
}

:/ OUTPUT
BB1: start
rsi = $0;
rbx = &([rbp - 40]);
rax = $5;
rax = rax * 4;
rdx = rax;
rdx movsx rdx;
rax = rbx;
rax = rax + rdx;
rcx = rax;
*(rcx) = rsi;
send
/: