#ifndef CPL_SYS_WAIT_H_
#define CPL_SYS_WAIT_H_ 0

#include "platform_h.cpl"
#include "sys_types_h.cpl"

#define WNOHANG    1
#define WUNTRACED  2
#ifdef CCPL_MACHO64
#define WCONTINUED 16
#endif
#ifndef CCPL_MACHO64
#define WCONTINUED 8
#endif

#ifdef CCPL_MACHO64
@[vname("_wait")] extern function wait(ptr i32 status) -> i32;
#endif
#ifndef CCPL_MACHO64
extern function wait(ptr i32 status) -> i32;
#endif
#ifdef CCPL_MACHO64
@[vname("_waitpid")] extern function waitpid(i32 pid, ptr i32 status, i32 options) -> i32;
#endif
#ifndef CCPL_MACHO64
extern function waitpid(i32 pid, ptr i32 status, i32 options) -> i32;
#endif

#endif
